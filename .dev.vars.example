# Copy to `.dev.vars` for local development. Production values go via `wrangler secret put`.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
RESEND_API_KEY="re_..."
GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'
