---
import PublicLayout from "~/layouts/PublicLayout.astro";
import { loadSettings } from "~/lib/queries";

const settings = await loadSettings(Astro);
const contactEmail = settings.contact_email;
---

<PublicLayout title="Contact" description="Get in touch with the Chuckanut Sports Car Club.">
  <div class="max-w-2xl mx-auto px-4 py-12">
    <h1 class="font-display text-4xl font-bold mb-2">Contact us</h1>
    <p class="text-zinc-400 mb-8">
      Questions about the club, membership, or events? Drop us a note.
      {contactEmail && (
        <> Or email us directly at{" "}
          <a href={`mailto:${contactEmail}`} class="text-brand-700 hover:underline">{contactEmail}</a>.
        </>
      )}
    </p>

    <form method="post" action="/api/contact" class="space-y-4 border border-zinc-800 rounded-lg p-6 bg-zinc-900">
      <label class="block">
        <span class="text-sm font-medium text-zinc-300">Your name</span>
        <input
          type="text"
          name="name"
          required
          class="mt-1 w-full border border-zinc-700 rounded px-3 py-2 text-sm bg-zinc-900"
        />
      </label>

      <label class="block">
        <span class="text-sm font-medium text-zinc-300">Email</span>
        <input
          type="email"
          name="email"
          required
          class="mt-1 w-full border border-zinc-700 rounded px-3 py-2 text-sm bg-zinc-900"
        />
      </label>

      <label class="block">
        <span class="text-sm font-medium text-zinc-300">Topic</span>
        <select name="topic" class="mt-1 w-full border border-zinc-700 rounded px-3 py-2 text-sm bg-zinc-900">
          <option value="general">General question</option>
          <option value="membership">Membership</option>
          <option value="events">Events / autocross</option>
          <option value="other">Other</option>
        </select>
      </label>

      <label class="block">
        <span class="text-sm font-medium text-zinc-300">Subject</span>
        <input
          type="text"
          name="subject"
          class="mt-1 w-full border border-zinc-700 rounded px-3 py-2 text-sm bg-zinc-900"
        />
      </label>

      <label class="block">
        <span class="text-sm font-medium text-zinc-300">Message</span>
        <textarea
          name="message"
          rows="5"
          required
          class="mt-1 w-full border border-zinc-700 rounded px-3 py-2 text-sm bg-zinc-900"
        ></textarea>
      </label>

      <button
        type="submit"
        class="bg-brand-700 text-white px-5 py-2 rounded font-medium hover:bg-brand-900"
      >
        Send message
      </button>
    </form>
  </div>
</PublicLayout>
